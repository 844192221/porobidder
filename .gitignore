backend/target/
backend/.idea/

# Optional per-machine Maven overrides (do not commit local paths)
backend/.mvn/maven.config
backend/.mvn/settings.xml

# Local DB secrets
backend/src/main/resources/application-local.properties

# Droplet deploy secrets
.env

# Editor / local tooling
.vscode/

# Database dumps (import manually on VPS)
*.sql
